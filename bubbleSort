#include <iostream>
using namespace std;
void bubbleSort(int a[], int n) {
int i,j;
for (int i = 0; i < n - 2; i++) {
for (int j = n-1; j >1; j--) {
if (a[j] < arr[j - 1]) {
swap(arr[j], arr[j - 1]);
}
}
}
}

int main() {
int n, a[100];
cin >> n;
for (int i = 0; i < n; i++) {
cin >> a[i];
}
bubbleSort(a, n);
for (int i = 0; i < n; i++) {
cout<<a[i]<<" ";
}
return 0;
}
